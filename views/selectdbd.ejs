<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>dbd</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.名前 %></td>
            <td><%= row.使用率 %></td>
            <td><%= row.殺傷率 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>